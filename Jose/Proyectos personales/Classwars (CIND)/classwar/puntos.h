#include <allegro.h>
#include <time.h>

void init();
void deinit();

class puntos()
{
    public:
        int puntos = 2000
        if(time < 100)
        {
            puntos++;
        }
        if (puntos >= 3000)
        {
            puntos = 3000;
        }

};
