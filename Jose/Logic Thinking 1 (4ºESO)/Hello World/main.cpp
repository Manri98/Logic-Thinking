#include <cstdlib>
#include <iostream>
#include <windows.h>

using namespace std;

int main()
{
    for (int sec=0; sec<60; sec++)
    {
        cout << sec << endl;
        Sleep(1000);
        system("cls");
        if (sec=59);
        Sleep(1000);
        for (int min; min<3; min++)
    }
    system("PAUSE");
    return EXIT_SUCCESS;
}
